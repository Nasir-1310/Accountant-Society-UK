// src/app/admin/news-blogs/edit/[id]/page.tsx
export default function EditNewsPlaceholder() {
  return null;
}



// // //src/app/admin/news-blogs/edit/[id]/page.tsx
// // import NewsForm from '@/components/admin/NewsForm';

// // interface Props {
// //   params: { id: string };
// // }

// // export default function EditNewsPage({ params }: Props) {
// //   return <NewsForm postId={params.id} />;
// // }






// import NewsForm from '@/components/admin/NewsForm';

// export default async function EditNewsPage({ params }: { params: { id: string } }) {
//   // No need to await anything here if you don't fetch data, but async fixes TS inference
//   return <NewsForm postId={params.id} />;
// }
